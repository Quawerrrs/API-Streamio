const express = require('express');
const router = express.Router();
const sessionController = require('../controllers/demandeController');

// router.post('/getDemandes', sessionController.getDemandes);

module.exports = router;